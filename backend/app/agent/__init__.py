"""The AI reasoning layer: a LangGraph pipeline that turns documents into checked data.

Conductor (M4) runs the business process around this; everything in here is the thinking part.
The graph is deliberately small and explicit so every step can be shown and explained.
"""
